#include <stdio.h>

int main() {
    int list[] = {5, 3, 8, 1, 2, 7};
    int temp, index, i, j;

    for(i=1; i<sizeof(list)/sizeof(int); i++){
    	key=list[i];
    	for(j=i;j>=0; &&list[j]>key; j--)
    	{
    		list[j+1]=list[i];
		}
		list[j+1]=key;
	}

	

    return 0;
}
