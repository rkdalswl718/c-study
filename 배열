#include <stdio.h>

int main() {
    int numbers[101]={};

    for (int i = 0; i < 100; i++) {
        scanf("%d ",&numbers[i]);
        if(numbers[i]==0){
            break;
        }

        if(i%2==1){
            printf("%d ",numbers[i]);
        }
    }
    return 0;
}
