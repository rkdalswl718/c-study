#include <stdio.h>

int main() {
    int numbers[10]={};

    for (int i = 0; i < 10; i++) {
        scanf("%d ",&numbers[i]);
    }

    printf("%d %d %d", numbers[2], numbers[4], numbers[9]);

    return 0;
}
