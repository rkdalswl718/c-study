#include <stdio.h>

int main() {
    char numbers[10]={};

    for (int i = 0; i < 10; i++) {
        scanf("%c ",&numbers[i]);
    }

    printf("%c %c %c", numbers[0], numbers[3], numbers[6]);

    return 0;
}
