#include <stdio.h>

int list[] = {};
int key, count=0, result;

int search (int key, int n){
	for(int i=0; i<n; i++){
	//	 count++;
		 if(key == list[i]) return i;
	}
	return -1; 
}

int main() {
	int n = sizeof(list)/sizeof(int);
	scanf("%d",&key)
	result = search(key,n);
	
	if(result==-1) printf("탐색실패");
	else printf("탐색성공/비교횟수: %d",count);
	return 0;
}
