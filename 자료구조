#include <stdio.h>

int n, arr[10000001]={0}, m, i, t, t1;

int main() {
	scanf("%d", &n);
	for (i=0; i<n; i++) {
		scanf("%d", &t);
		arr[t] = 1;
	}
	
	scanf("%d", &m);
	for (i=0; i<m; i++) {
		scanf("%d", &t);
		if (arr[t]) {
			printf("1 ");
		} else {
			printf("0 ");
		}
	}
	return 0;
}
