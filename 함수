#include <stdio.h>
#include <math.h>  // math.h 라이브러리 포함

void printnumber(int a, int b) {
    printf("%d", (int)round(pow(a, b)));  // pow() 함수 사용
}

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    
    printnumber(a, b);

    return 0;
}
