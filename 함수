#include <stdio.h>

void printNumbers(int num) {
    printf("10큰수 : %d\n", num + 10);
    printf("10작은수 : %d", num - 10);
}

int main() {
    int num;
    scanf("%d", &num);

    printNumbers(num);

    return 0;
}
